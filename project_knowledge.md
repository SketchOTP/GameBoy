# Project Knowledge

Fallback durable lessons only when Mimir MCP is unavailable.

Do not store the same fact in Mimir and this file unless explicitly required.

## Bootstrap (2026-06-17)

- Repo is greenfield: only `.serena/project.yml` existed before bootstrap; no application manifest yet.
- Intended direction: DMG Game Boy emulator (see `project_goals.md`).
- Navigation: use `repo_map.md` and `project_memory/index.json`; no `src/` tree until stack is chosen.
- Verification commands TBD — inspect root for `Cargo.toml`, `package.json`, or `Makefile` before inventing test commands.
- Serena project name is `GameBoy`; languages list empty until toolchain is added.
